1. STL
   stack: push, pop, top
   vector: push_back
   
2. Constructor 
   struct TreeNode {
      int val;
      TreeNode *left;
      TreeNode *right;
      TreeNode(int x) : val(x), left(NULL), right(NULL) {}
  };
  
3. int to string
   to_string(42) /*Don't forget include string in the head file*/
